import chalk from "chalk";

console.log(chalk.blue.bold("=== Chalk Project ==="));

console.log(
    chalk.green("✔ Repository successfully created!")
);

console.log(
    chalk.yellow("✔ This message uses Chalk colors.")
);

console.log(
    chalk.magenta.bold("Node.js + Git + GitHub working together!")
);